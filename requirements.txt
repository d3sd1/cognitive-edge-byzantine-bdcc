# Pinned to the exact versions installed in the reproduction venv on the
# reference host (AMD Ryzen 9 5900X, Windows 11, Python 3.13.7). torch is the
# CPU build (torch==2.12.0+cpu); install with:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
scipy==1.17.1
matplotlib==3.11.0
openpyxl==3.1.5
torch==2.12.0
